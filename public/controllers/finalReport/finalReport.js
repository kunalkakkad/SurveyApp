angular.module('MyApp')
    .controller('finalReport', function($rootScope, $scope, Account, $http, $uibModal,toastr,$interval,$timeout,$ngConfirm,ModalService,MDL) {
     
    // this.uiCanExit = function() {
    // //   if (!angular.equals(this.data, this.originalData)) {
    //     // Note: This could also return a Promise and request async
    //     // confirmation using something like ui-bootstrap $modal
    
    //     return window.confirm("You have unsaved changes.  Exit anyway and lose changes?");
        
    // };
});